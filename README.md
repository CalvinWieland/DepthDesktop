# DepthDesktop

`/app` : Main lifecycle & entry point

`/ui` : windowing (Qt) & interactions

`/rendering` : ModernGL renderer + camera

`/tracking` : MediaPipe, head pose estimation

Made for the 2026 CrimsonCode Hackathon at WSU. 
 
